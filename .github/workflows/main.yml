name: CI/CD Workflow

on:
  push:
    branches:
      - main
      - dev

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: node:16.14.0

    steps:
      - uses: actions/checkout@v2

      - name: Cache node modules
        uses: actions/cache@v2
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-
            
      - name: Install dependencies
        run: npm i --production

  security_scan:
    needs: build
    runs-on: ubuntu-latest
    container:
      image: node:16.14.0

    steps:
      - uses: actions/checkout@v2

      - name: Restore cache
        uses: actions/cache@v2
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-
            
      - name: Install dependencies
        run: npm i
        
      - name: Security Scan
        uses: zricethezav/gitleaks-action@v1.5.0

  deploy_main:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs: [build, security_scan]
    environment:
      name: production
      url: https://production.trimworldwide.com  # Replace with your production URL
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16.14.0'
          
      - name: Install dependencies
        run: npm i

      - name: 📦 Archive files
        run: tar -czvf deploy.tar.gz .

      - name: 📂 Sync files to main domain
        uses: appleboy/scp-action@v0.1.1
        with:
          host: ftp.trimworldwide.com
          username: production
          key: ${{ secrets.SFTP_PRIVATE_KEY }}
          target: "/home/trimworldwide/production.trimworldwide.com"  # Adjust the target path
          source: "deploy.tar.gz"
          port: 22
          timeout: 30s
          command_timeout: 10m
          use_insecure_cipher: false
          rm: false
          debug: false
          strip_components: 0
          overwrite: false
          proxy_port: 22
          proxy_timeout: 30s
          proxy_use_insecure_cipher: false

  deploy_dev:
    if: github.ref == 'refs/heads/dev'
    runs-on: ubuntu-latest
    needs: [build, security_scan]
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16.14.0'
          
      - name: Install dependencies
        run: npm i

      - name: 📦 Archive files
        run: tar -czvf deploy.tar.gz .

      - name: 📂 Sync files to subdomain
        uses: appleboy/scp-action@v0.1.1
        with:
          host: ftp.trimworldwide.com
          username: dev
          key: ${{ secrets.SFTP_PRIVATE_KEY }}
          target: "/home/trimworldwide/dev.trimworldwide.com"  # Adjust the target path
          source: "deploy.tar.gz"
          port: 22
          timeout: 30s
          command_timeout: 10m
          use_insecure_cipher: false
          rm: false
          debug: false
          strip_components: 0
          overwrite: false
          proxy_port: 22
          proxy_timeout: 30s
          proxy_use_insecure_cipher: false
